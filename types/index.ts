// Re-export all types from the main types file
export { Role, type Message, type User, type CodeVersion, type Skill, type ProjectTemplate, type StyleFramework, type Theme, type ClaudeSettings, type ChatSession, type ChatState, type CodeUpdateCallback } from '../types';

// Re-export canvas types
export * from './canvas';